# 🔄 طرق بديلة للنشر

## المشكلة: SSH غير متاح

إذا كان SSH غير متاح، يمكنك استخدام إحدى الطرق التالية:

---

## الطريقة 1: استخدام WinSCP (Windows) ⭐ موصى به

### الخطوات:

1. **حمّل WinSCP:**
   - https://winscp.net/eng/download.php
   - أو استخدم Chocolatey: `choco install winscp`

2. **اتصل بالخادم:**
   - File protocol: `SFTP`
   - Host name: `95.216.63.80`
   - Port number: `22` (أو المنفذ الصحيح)
   - User name: `root`
   - Password: [كلمة مرور الخادم]
   - اضغط "Login"

3. **انسخ الملفات:**
   - في الجانب الأيسر: مجلد المشروع `G:\ATM Master Pro\test`
   - في الجانب الأيمن: `/var/www/atm-master-pro`
   - حدد جميع الملفات (Ctrl+A)
   - اسحب وأفلت أو اضغط F5

---

## الطريقة 2: استخدام FileZilla

1. **حمّل FileZilla:**
   - https://filezilla-project.org/download.php?type=client

2. **اتصل بالخادم:**
   - Host: `sftp://95.216.63.80`
   - Username: `root`
   - Password: [كلمة مرور الخادم]
   - Port: `22`

3. **انسخ الملفات** كما في WinSCP

---

## الطريقة 3: استخدام لوحة التحكم (cPanel/Plesk)

إذا كان لديك وصول إلى cPanel أو Plesk:

1. سجل دخول إلى لوحة التحكم
2. افتح **File Manager**
3. اذهب إلى `/var/www/atm-master-pro`
4. ارفع الملفات باستخدام **Upload** أو **Drag & Drop**

---

## الطريقة 4: استخدام Git (إذا كان متاحاً)

### على جهازك المحلي:

```bash
# إضافة جميع الملفات
git add .
git commit -m "Deploy to production"
git push origin main
```

### على الخادم (بعد الوصول بأي طريقة):

```bash
cd /var/www
git clone <your-repo-url> atm-master-pro
cd atm-master-pro
```

---

## الطريقة 5: استخدام RDP (إذا كان الخادم Windows)

1. اتصل بالخادم باستخدام Remote Desktop
2. افتح File Explorer
3. انسخ الملفات مباشرة

---

## بعد رفع الملفات (بأي طريقة)

بعد رفع الملفات بنجاح، سجل دخول إلى الخادم (SSH، Terminal، أو أي طريقة متاحة) ونفّذ:

```bash
# الانتقال إلى المجلد
cd /var/www/atm-master-pro

# التأكد من وجود ملف .env.production
cat .env.production

# جعل السكريبتات قابلة للتنفيذ
chmod +x deploy.sh
chmod +x create-env-production.sh

# تشغيل سكريبت النشر
./deploy.sh

# تثبيت PM2
npm install -g pm2

# تشغيل التطبيق
pm2 start ecosystem.config.js
pm2 save
pm2 startup
```

---

## 🎯 التوصية

**استخدم WinSCP** - إنه الأسهل والأسرع على Windows!

---

**بعد رفع الملفات، أكمل الخطوات 3 و 4 من EXECUTE_DEPLOYMENT.md** 🚀

