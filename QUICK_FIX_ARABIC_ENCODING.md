# 🔧 حل سريع: الأسماء العربية لا تظهر بعد استعادة النسخة الاحتياطية

## ⚡ الحل السريع (3 خطوات)

### 1️⃣ فحص المشكلة
افتح في المتصفح:
```
http://localhost:9002/api/database/check-encoding
```

### 2️⃣ تنفيذ سكربت الإصلاح
افتح **SQL Server Management Studio** وقم بـ:
1. الاتصال بقاعدة البيانات
2. فتح ملف: `prisma/fix-arabic-encoding-after-restore.sql`
3. تنفيذ السكربت

### 3️⃣ إعادة تشغيل التطبيق
```bash
npm run dev
```

---

## 📋 الحل التفصيلي

للحل الشامل والتفصيلي، راجع:
- **`FIX_ARABIC_AFTER_BACKUP_RESTORE.md`** - دليل شامل خطوة بخطوة

---

## ⚠️ ملاحظات مهمة

- إذا كانت البيانات تظهر كعلامات استفهام `?????`، فهذا يعني أن البيانات **تالفة** ولا يمكن استرجاعها
- يجب إعادة استيراد البيانات التالفة من ملف Excel/CSV الأصلي
- عند إدخال بيانات عربية جديدة في SQL، استخدم حرف `N` قبل النص: `N'النص العربي'`

---

## 🆘 إذا استمرت المشكلة

1. افتح `/api/database/check-encoding` وأرسل النتيجة
2. تحقق من أن النسخة الاحتياطية الأصلية كانت تحتوي على بيانات صحيحة
3. راجع ملف `FIX_ARABIC_AFTER_BACKUP_RESTORE.md` للحلول الإضافية

