# 🔧 حل مشاكل الاتصال بالخادم

## المشكلة: Connection timed out على SSH

### الحلول الممكنة:

### 1️⃣ التحقق من الاتصال بالخادم

```bash
# اختبار ping
ping 95.216.63.80

# اختبار المنفذ 22 (SSH)
telnet 95.216.63.80 22
```

### 2️⃣ استخدام منفذ SSH مختلف

إذا كان SSH يعمل على منفذ آخر (مثل 2222):

```bash
scp -P 2222 -r . root@95.216.63.80:/var/www/atm-master-pro
```

### 3️⃣ استخدام SFTP/FTP

إذا كان لديك وصول FTP/SFTP:

**Windows - استخدام WinSCP:**
1. حمّل WinSCP من: https://winscp.net
2. اتصل بـ:
   - Host: 95.216.63.80
   - Username: root
   - Password: [كلمة مرور الخادم]
   - Protocol: SFTP أو FTP
3. انسخ جميع الملفات إلى `/var/www/atm-master-pro`

**Windows - استخدام FileZilla:**
1. حمّل FileZilla من: https://filezilla-project.org
2. اتصل بالخادم
3. انسخ الملفات

### 4️⃣ استخدام لوحة التحكم (cPanel/Plesk)

إذا كان لديك وصول إلى لوحة التحكم:
1. سجل دخول إلى لوحة التحكم
2. استخدم File Manager
3. ارفع الملفات إلى `/var/www/atm-master-pro`

### 5️⃣ استخدام Git (إذا كان متاحاً)

إذا كان الخادم يدعم Git:

```bash
# على الخادم
cd /var/www
git clone <your-repo-url> atm-master-pro
cd atm-master-pro
```

### 6️⃣ فتح منفذ SSH في Firewall

إذا كان لديك وصول إلى الخادم بطريقة أخرى:

```bash
# على الخادم
sudo ufw allow 22/tcp
# أو
sudo firewall-cmd --permanent --add-service=ssh
sudo firewall-cmd --reload
```

### 7️⃣ استخدام RDP أو VNC

إذا كان الخادم Windows أو لديك وصول سطح المكتب:
- استخدم Remote Desktop (RDP) للاتصال
- ثم انسخ الملفات مباشرة

---

## 🎯 الحل الموصى به

### الطريقة الأسهل: استخدام WinSCP أو FileZilla

1. **حمّل WinSCP**: https://winscp.net/eng/download.php
2. **اتصل بالخادم**:
   - Host name: `95.216.63.80`
   - Port: `22` (أو المنفذ الصحيح)
   - User name: `root`
   - Password: [كلمة مرور الخادم]
   - Protocol: `SFTP`
3. **انسخ الملفات**:
   - من جهازك: `G:\ATM Master Pro\test`
   - إلى الخادم: `/var/www/atm-master-pro`

---

## 📋 بعد رفع الملفات

بعد رفع الملفات بنجاح، سجل دخول إلى الخادم (بأي طريقة متاحة) ونفّذ:

```bash
cd /var/www/atm-master-pro
chmod +x deploy.sh
./deploy.sh
npm install -g pm2
pm2 start ecosystem.config.js
pm2 save
pm2 startup
```

---

## ❓ أسئلة شائعة

**س: كيف أعرف المنفذ الصحيح لـ SSH؟**
- ج: جرب المنافذ الشائعة: 22, 2222, 2200
- أو اسأل مدير الخادم

**س: هل يمكنني استخدام FTP بدلاً من SFTP؟**
- ج: نعم، لكن SFTP أكثر أماناً

**س: ماذا لو لم يكن لدي وصول SSH؟**
- ج: استخدم لوحة التحكم (cPanel/Plesk) أو اتصل بمدير الخادم

---

**اختر الطريقة التي تناسبك واتبع الخطوات أعلاه!** 🚀

