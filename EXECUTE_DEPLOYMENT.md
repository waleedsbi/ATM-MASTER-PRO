# 🚀 تنفيذ خطوات النشر على الخادم

## ✅ الخطوة 1: تمت محلياً
- تم إنشاء ملف `.env.production` بنجاح ✓

## 📋 الخطوات المتبقية (على الخادم)

### الخطوة 2: رفع الملفات إلى الخادم

**من جهازك المحلي (PowerShell أو CMD):**

```powershell
# رفع جميع الملفات إلى الخادم
scp -r . root@95.216.63.80:/var/www/atm-master-pro

# أو إذا كان لديك اسم مستخدم مختلف
scp -r . user@95.216.63.80:/var/www/atm-master-pro
```

**ملاحظة:** قد يطلب منك إدخال كلمة مرور SSH

---

### الخطوة 3: على الخادم - إعداد وتشغيل النظام

**تسجيل الدخول إلى الخادم:**

```bash
ssh root@95.216.63.80
# أو
ssh user@95.216.63.80
```

**بعد تسجيل الدخول، نفّذ الأوامر التالية:**

```bash
# 1. الانتقال إلى مجلد المشروع
cd /var/www/atm-master-pro

# 2. التأكد من وجود ملف .env.production
cat .env.production

# 3. جعل السكريبتات قابلة للتنفيذ
chmod +x deploy.sh
chmod +x create-env-production.sh

# 4. تشغيل سكريبت النشر (سيقوم بـ):
#    - تثبيت المتطلبات
#    - توليد Prisma Client
#    - تحديث قاعدة البيانات
#    - بناء المشروع
./deploy.sh

# 5. تثبيت PM2 (إذا لم يكن مثبتاً)
npm install -g pm2

# 6. تشغيل التطبيق باستخدام PM2
pm2 start ecosystem.config.js

# 7. حفظ الإعدادات
pm2 save

# 8. إعداد PM2 للبدء تلقائياً عند إعادة تشغيل الخادم
pm2 startup
# ثم شغّل الأمر الذي يظهر لك (مثل: sudo env PATH=$PATH:/usr/bin pm2 startup systemd -u root --hp /root)
```

---

### الخطوة 4: التحقق من النشر

**على الخادم:**

```bash
# فحص حالة PM2
pm2 status

# فحص السجلات
pm2 logs atm-master-pro

# فحص الاتصال بقاعدة البيانات
cd /var/www/atm-master-pro
npx prisma db pull
```

**في المتصفح:**

افتح: **https://springtradingclean.com**

**تسجيل الدخول:**
- Email: `admin@atmpro.com`
- Password: `admin123`

---

## 🔧 إعداد Nginx (اختياري - إذا لم يكن مُعداً)

```bash
# على الخادم
sudo cp /var/www/atm-master-pro/nginx.conf.example /etc/nginx/sites-available/atm-master-pro
sudo ln -s /etc/nginx/sites-available/atm-master-pro /etc/nginx/sites-enabled/
sudo nginx -t
sudo systemctl reload nginx
```

---

## ⚠️ استكشاف الأخطاء

### إذا لم يعمل التطبيق:

```bash
# فحص السجلات
pm2 logs atm-master-pro --lines 100

# إعادة تشغيل التطبيق
pm2 restart atm-master-pro

# فحص المنافذ
netstat -tulpn | grep 9002
```

### إذا كان هناك خطأ في قاعدة البيانات:

```bash
# اختبار الاتصال
cd /var/www/atm-master-pro
npx prisma db pull

# إعادة توليد Prisma Client
npx prisma generate
```

---

## 📝 ملاحظات مهمة

1. **الأمان**: بعد النشر، غيّر كلمة مرور المستخدم الافتراضي
2. **Firewall**: تأكد من فتح المنفذ 9002 في Firewall
3. **HTTPS**: يُنصح بإعداد SSL Certificate (Let's Encrypt) لـ HTTPS

---

**جاهز للتنفيذ! ابدأ بالخطوة 2 أعلاه.** 🚀

